import { FiltroNombreProyectoPipe } from './filtro-nombre-proyecto.pipe';

describe('FiltroNombreProyectoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroNombreProyectoPipe();
    expect(pipe).toBeTruthy();
  });
});
