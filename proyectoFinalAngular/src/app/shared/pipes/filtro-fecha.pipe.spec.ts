import { FiltroFechaPipe } from './filtro-fecha.pipe';

describe('FiltroFechaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroFechaPipe();
    expect(pipe).toBeTruthy();
  });
});
