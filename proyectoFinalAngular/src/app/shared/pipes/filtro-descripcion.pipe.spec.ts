import { FiltroDescripcionPipe } from './filtro-descripcion.pipe';

describe('FiltroDescripcionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroDescripcionPipe();
    expect(pipe).toBeTruthy();
  });
});
