export class Usuario{

    constructor( public username: string, public pass: string, public nombre: string, public apellidos: string, public email: string, public visible: number, public enabled: number){  }
}